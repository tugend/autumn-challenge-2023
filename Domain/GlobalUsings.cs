// Global using directives

// The downside with this is that externally and internally this now looks different, and we don't have anywhere to put the documentation
// global using Parse = System.Func<string, Domain.Models.State>;
