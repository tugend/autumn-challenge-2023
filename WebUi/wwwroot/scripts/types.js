/**
 * @typedef {object} CatalogEntry
 * @property {string} key
 * @property { int[][] } value
 */

/**
 * @typedef {object} State
 * @property {number} turn
 * @property { int[][]} grid
 *
 * @param {State} seed
 */

/**
 * @typedef { object } DomClient 
 * @property { any } renderTo
 * @property { any } rerender
 * @property { any } subscribe
 */